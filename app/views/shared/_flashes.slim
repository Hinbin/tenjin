- flash.each do |type, msg|
  - if msg.is_a?(String)
    .alert.alert-dismissible.fade.show class=bootstrap_flash_class(type) role="alert"
      = msg
      button.btn-close type="button" data-bs-dismiss="alert" aria-label="Close"
